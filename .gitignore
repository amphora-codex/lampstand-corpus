# ── Compiled corpus artifacts — NEVER committed (built, not source) ──
output/
output/packs/   # P7 bundled + on-demand pack .sqlite files (built; manifest is committed)
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.embeddings
*.index
*.faiss
*.npy

# ── Downloaded model weights (e.g. BGE-small) ──
models/

# ── Python ──
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# ── macOS / editor ──
.DS_Store
.vscode/
.idea/

# scratch markers/logs from detached builds
reports/.p6_done
reports/.p6_encode.log
reports/.*.log

